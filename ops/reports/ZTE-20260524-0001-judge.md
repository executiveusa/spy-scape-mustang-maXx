# ZTE-20260524-0001 Judge Pass

## Inputs Reviewed

- `prd/wave-3-operator-access/PRD.md`
- `AGENTS.md`
- Local diff on `codex/operator-control-room`
- Worker report `ops/reports/ZTE-20260524-0001.json`
- Visual screenshots under `ops/visual-inspection/2026-05-24T10-05-53-046Z/`
- Local verification outputs for backend tests, contract tests, TypeScript, build, and visual inspection

## Findings

- Blocking: none.
- Nitpick: dashboard local visual state shows degraded runtime cards when the BFF shared secret is not provided. This is acceptable for local visual verification because the UI still renders the protected operator shell and the live backend smoke remains a separate launch gate.
- Accepted risk: the shared navigation is a shell consolidation, not a full operator analytics redesign. Deeper task analytics and tenant onboarding polish stay in later product slices.

## Acceptance Check

- Operator pages share a branded Agent MAXX backend UI shell.
- Lead Desk, Lead Acquisition, tenants, deploy, and dashboard are linked from the shell.
- Login no longer depends on client router state after the session cookie is issued.
- Browser-facing API calls match the app's trailing-slash routing policy.
- Visual inspection waits for hydrated controls and route navigation before judging protected pages.
- No visible product surface exposes the private runtime vendor name.

## Approval

Approved for `land-the-plane`.

